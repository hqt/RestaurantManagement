<%= form_for(@temp_table) do |f| %>
  <% if @temp_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_table.errors.count, "error") %> prohibited this temp_table from being saved:</h2>

      <ul>
      <% @temp_table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
